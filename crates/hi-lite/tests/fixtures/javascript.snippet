// A complete JavaScript fixture with a template literal.
class User {
  constructor(name, count = 42) {
    this.name = name;
    this.count = count;
  }

  label() {
    return `${this.name} (${this.count})`;
  }
}

export const user = new User("Ada");
