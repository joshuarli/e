"""A complete Python fixture with a multiline docstring."""
from dataclasses import dataclass

@dataclass
class User:
    name: str
    count: int = 42

    def label(self) -> str:
        return f"{self.name} ({self.count})"

if __name__ == "__main__":
    print(User("Ada").label())  # call a method
