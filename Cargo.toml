[package]
name = "e"
version = "0.1.0"
edition = "2024"

[dependencies]
termion = "4"
regex = "1"
libc = "0.2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
