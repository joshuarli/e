[package]
name = "e"
version = "0.1.13"
edition = "2024"

[dependencies]
laputa-hi-lite = "0.1"
fxhash = "0.2"
regex-lite = "0.1"
rustix = { version = "1.1.4", features = ["runtime", "termios"] }

[dev-dependencies]
rustybench = "0.1"
laputa-ptytest = "0.1.1"
proptest = "1"
# Dev-only tooling: `examples/gen_xsh.rs` parses the XSH registry source with
# `syn` to regenerate src/xsh_lang.rs; `tests/xsh_lang.rs` verifies freshness.
syn = { version = "2", features = ["full", "visit"] }
proc-macro2 = "1"

[[bench]]
name = "bench"
harness = false

[profile.dev]
debug = 0
strip = "debuginfo"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
